[build]
pkgs = ["nodejs"]

[phases.install]
cmds = ["npm install -g bun", "bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "npm install -g serve && serve -s dist -l 3000"
